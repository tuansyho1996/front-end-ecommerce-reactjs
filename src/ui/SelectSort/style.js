import styled from "styled-components";
import { Select } from "@mui/material"

const SelectSortStyled = styled(Select)`
  .MuiSelect-icon{
    color:var(--text)
  }
  .MuiSelect-select{
    padding:8px 10px 8px 10px
  }
`
export default SelectSortStyled
